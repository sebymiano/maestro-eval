[tool.poetry]
name = "scr-pcaps"
version = "0.1.0"
description = "Set of scripts to generate SCR compatible pcaps"
authors = ["Sebastiano Miano"]

[tool.poetry.dependencies]
python = "^3.10"
scapy = "^2.6.0"
argparse = "^1.4.0"
black = "^24.10.0"
tqdm = "^4.66.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
